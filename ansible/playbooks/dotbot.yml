---
- name: Dotbot Symlinks
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  roles:
    - role: opencode
      tags: [opencode, secrets]
    - role: dotbot
      tags: [dotbot, symlinks]
