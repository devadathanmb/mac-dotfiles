---
- name: Ensure Homebrew is installed (Apple Silicon)
  ansible.builtin.command:
    cmd: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    creates: /opt/homebrew/bin/brew
  when:
    - ansible_facts['os_family'] == "Darwin"
    - ansible_facts['architecture'] == "arm64"
  become: false
  environment:
    NONINTERACTIVE: "1"

- name: Ensure Homebrew is installed (Intel)
  ansible.builtin.command:
    cmd: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    creates: /usr/local/bin/brew
  when:
    - ansible_facts['os_family'] == "Darwin"
    - ansible_facts['architecture'] != "arm64"
  become: false
  environment:
    NONINTERACTIVE: "1"

- name: Update Homebrew
  community.general.homebrew:
    update_homebrew: true
  become: false

- name: Read Homebrew formulae from file
  ansible.builtin.set_fact:
    brew_formulae: "{{ lookup('file', dotfiles_repo + '/homebrew/brew_packages.txt').splitlines() | reject('==', '') | list }}"

- name: Read Homebrew casks from file
  ansible.builtin.set_fact:
    brew_casks: "{{ lookup('file', dotfiles_repo + '/homebrew/brew_casks.txt').splitlines() | reject('==', '') | list }}"

- name: Install Homebrew formulae
  community.general.homebrew:
    name: "{{ item }}"
    state: present
  loop: "{{ brew_formulae }}"
  become: false

- name: Validate sudo before installing privileged casks
  ansible.builtin.command: /usr/bin/true
  become: true
  changed_when: false

- name: Install Homebrew casks
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: present
    sudo_password: "{{ bootstrap_sudo_password }}"
  loop: "{{ brew_casks }}"
  become: false

- name: Resolve Homebrew command conflicts
  ansible.builtin.import_tasks: command_conflicts.yml

- name: Configure default applications
  ansible.builtin.command:
    argv:
      - duti
      - "{{ dotfiles_repo }}/configs/duti/defaults.duti"
  changed_when: false
